services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: uni_asset_app
    ports:
      - "5432:5432"
    volumes:
      - uni_asset_app_pg:/var/lib/postgresql/data

volumes:
  uni_asset_app_pg:

